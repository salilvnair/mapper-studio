# Azure2AWS Mapping Samples

Cloud platform scenarios only.

Each scenario has:
- `aws-source-request.xml` (source payload)
- `azure-target.xsd` (target schema)
- `azure-target.wsdl` (target contract)

Scenarios:
1. `compute-start-vm`
2. `messaging-publish`
3. `monitoring-metric-alert`
